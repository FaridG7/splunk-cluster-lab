{# =========================================================================== #}
{# server.conf.j2 — splunk_deployment_server                                 #}
{#                                                                            #}
{# Owns ONLY: [deployment-server]                                            #}
{# mgmt-0 merges this with role_cluster_manager/local/server.conf at        #}
{# runtime. The two stanzas are completely independent.                      #}
{# =========================================================================== #}

[deployment-server]
disabled = false
